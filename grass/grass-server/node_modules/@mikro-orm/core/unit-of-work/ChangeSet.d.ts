import { EntityData, AnyEntity, EntityMetadata } from '../typings';
export declare class ChangeSet<T extends AnyEntity<T>> {
    entity: T;
    type: ChangeSetType;
    payload: EntityData<T>;
    constructor(entity: T, type: ChangeSetType, payload: EntityData<T>, meta: EntityMetadata<T>);
}
export interface ChangeSet<T extends AnyEntity<T>> {
    name: string;
    rootName: string;
    collection: string;
    type: ChangeSetType;
    entity: T;
    payload: EntityData<T>;
    persisted: boolean;
    originalEntity?: EntityData<T>;
}
export declare enum ChangeSetType {
    CREATE = "create",
    UPDATE = "update",
    DELETE = "delete"
}
