mutation Register($username: String!, $password: String!) {
  register(options: { username: $username, password: $passwordy }) {
    errors {
      field
      message
    }
    user {
      ...RegularUser
    }
  }
}
